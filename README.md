# mariage-corblet-montes.github.io


```
brew cleanup
brew upgrade npm
npm install
brew install hugo
hugo serve --themesDir themes --panicOnWarning
hugo serve --themesDir themes --disableFastRender
hugo serve  --navigateToChanged
```